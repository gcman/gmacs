# -*- mode: snippet -*-
# name: bar
# key: math-bar
# type: command
# --
(let ((len (length (gm/last-letter))))
(if (> len 0)
(progn
  (backward-char len)
  (insert "\\bar{")
  (forward-char len)
  (insert "}"))
 (yas-expand-snippet "\\bar{$1}")))